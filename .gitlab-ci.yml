image: node:latest

stages:
    - default
    - build
    - lint

default:
    before_script:
        - export PATH=$PATH:/usr/local/bin
        - npm install
    tags:
        - moneydog

client_build_test:
    stage: build
    script:
        - echo 'client build start'
        - npm run build
    tags:
        - moneydog

client_lint_test:
    stage: lint
    script:
        - echo 'client lint start'
        - npm run lint
    tags:
        - moneydog


