image: node:latest

stages:
    - lint

eslint:
    only:
    - yh-issue-42
    stage: lint
    script:
        - echo eslint start
        - cd server
        - npm install -g eslint
        - eslint --init
        - eslint app.js